# 02-bloom — build rules
Build ONLY what is in @SPEC.md. Anything beyond the Scope Cap → STOP, flag as Question.
Inherit all global rules from the root CLAUDE.md (Phaser 4 only, primitives-first, reuse template/).
The rules are FIXED Conway B3/S23 — do not invent, tune, or "improve" them. Double-buffer for real (swap A/B, never mutate in place).
Reuse from ../../template: scenes skeleton, feel/, lib/ (input, score). If you build a reusable grid helper, add it to ../../template generically.
Status every step: Phase / Step / Plan / Blocker / Question.
@SPEC.md
